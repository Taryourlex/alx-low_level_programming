#include "main.h"
#include <stdio.h>

/**
 * int mul - multiply two integers
 * @a: The first integer
 * @b: The second one
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
